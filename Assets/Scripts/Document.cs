using UnityEngine;
using TMPro;

public class Document : MonoBehaviour
{
	public GameObject searchWarrantText;
	public TMP_Text warrantName;
	public TMP_Text warrantAddress;
	public TMP_Text warrantSignature;
	public TMP_Text warrantDate;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
